fun main(){
    /*
    lamda expression is a function which has no name
    lamda expressions and anonymous functions are 'function literals' , ie. functions that are not declared , but passed immediately as an expression
    lamda is defined with curly braces {} which takes variables as parameter (if any) and body of a function
     */
    val a = 1

   // print(sum(2,2))
}
// sample of normal function
fun addNumbers(a:Int , b:Int) {
    val add = a+b
}
// sample of lamda expression
//val sum : (Int, Int) -> Int = (val a:Int , val b:Int -> a+b)